import { MarkdownTooltipDirective } from './markdown-tooltip.directive';

describe('MarkdownTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkdownTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
